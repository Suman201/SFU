window.__SFU_ENV__ = window.__SFU_ENV__ || {
  // Optional runtime overrides for split-host deployments.
  // Leave empty for same-origin production ingress (/api/v1 and /sfu).
  // Never ship localhost values in production; non-local browsers ignore them.
  // Example:
  // apiBaseUrl: 'https://staging.example.com/api/v1',
  // socketUrl: 'https://staging.example.com/sfu'
};
